---
"alliance-platform-ui": patch
---

Fix static `number_input` native form submission by attaching the reusable Alliance UI NumberInput runtime, keep validation icons mutually exclusive with step controls, and prevent inline icon dependencies from rendering as detached document images.
