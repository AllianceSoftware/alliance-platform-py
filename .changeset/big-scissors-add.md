---
"alliance-platform-frontend": patch
---

Handle converting attributes on `input` from html => React correctly. Specifically `value` => `defaultValue`, `checked` => `defaultChecked` and `autofocus` => `autoFocus`.
