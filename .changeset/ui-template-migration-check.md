---
"alliance-platform-ui": patch
---

Add the opt-in `ui_migration_check` Django command for locating legacy Alliance UI template
components and assessing their static-renderer or native HTML migration path.
